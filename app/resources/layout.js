export const metadata = {
  title: 'StudentHub - Learning Resources',
  description: 'Browse courses and study materials curated for students across design, coding, business, and more.',
}

export default function ResourcesLayout({ children }) {
  return children
}
